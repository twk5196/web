함수만들기 
function 함수이름 (매개변수 ){
		...
{


!! classList 클래스 목록으로 클래스 삭제,추가등 다양한 메서드 사용가능
   classList.contains( ) : 클래스 값이 존재하는지 체크하는메서드 ,불리안
		   반환 
   classList.toggle( ) : 클래스 값이 있는지 체크하고 없으면 추가 있으면
		삭제한다. 

시계 업데이트 함수 
시계 함수는 직접만들고 (시분초)
setInterval( 시계함수, 업데이트 단위(밀리세컨))
사용 

삼항 연산자 : 
변수 <(연산자) 수치 ? 참일경우반환값:거짓일경우반환값